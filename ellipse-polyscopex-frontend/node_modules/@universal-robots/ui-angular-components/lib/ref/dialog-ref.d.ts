import { OverlayRef } from '@angular/cdk/overlay';
import { FocusTrap } from 'focus-trap';
import { Observable } from 'rxjs';
import { CloseDialog, DialogConfig } from '@universal-robots/ui-models';
import { DialogAnimationState } from '../animations';
import * as i0 from "@angular/core";
export interface DialogRefInterface {
    cleanUp: () => void;
}
export declare class DialogRef<T> implements DialogRefInterface {
    private readonly overlayRef;
    readonly id: string;
    readonly config: DialogConfig;
    disableClose: boolean;
    private localStorageHelper;
    private isAnimatedPassive;
    private animationState$;
    private afterClose$;
    private afterOpen$;
    private afterCleanUp$;
    private subscriptions$;
    private _focusTrap?;
    constructor(overlayRef: OverlayRef, id: string, config: DialogConfig);
    /** Gets an observable that emits when dialog is finished closing. */
    get afterClose(): Observable<CloseDialog<T>>;
    /** Gets an observable that emits when dialog is finished opening. */
    get afterOpen(): Observable<string>;
    /** Gets an observable that emits when overlay has been attached. */
    get animationState(): Observable<DialogAnimationState>;
    get afterCleanUp(): Observable<string>;
    get overlayRefElement(): HTMLElement;
    get focusTrap(): FocusTrap | undefined;
    setAnimationState(state: DialogAnimationState): void;
    close(data?: CloseDialog<T>): void;
    /**
     * Clean up after the dialog has been removed from the DOM.
     */
    cleanUp(): void;
    private readonly animationEffect;
    private onAnimationStateChange;
    /**
     * Force close the dialog callback to be used in eventlistener
     * @param event
     * @returns
     */
    private readonly forceClose;
    private readonly closeByErrorType;
    private handleSafetyModeErrorDialogs;
    private readonly closeById;
    /**
     * Close the dialog when the backdrop is clicked
     */
    private readonly onBackdropClick;
    static ɵfac: i0.ɵɵFactoryDeclaration<DialogRef<any>, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<DialogRef<any>>;
}
