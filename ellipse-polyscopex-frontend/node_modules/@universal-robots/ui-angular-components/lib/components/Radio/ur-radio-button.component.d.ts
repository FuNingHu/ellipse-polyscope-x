import { FocusMonitor, FocusOrigin } from '@angular/cdk/a11y';
import { UniqueSelectionDispatcher } from '@angular/cdk/collections';
import { AfterContentInit, AfterViewInit, ChangeDetectorRef, ElementRef, EventEmitter, InjectionToken, OnDestroy, OnInit, QueryList } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
import * as i0 from "@angular/core";
/**
 * Provider Expression that allows ur-radio-group to register as a ControlValueAccessor. This
 * allows it to support [(ngModel)] and ngControl.
 * @docs-private
 */
export declare const UR_RADIO_GROUP_CONTROL_VALUE_ACCESSOR: any;
/** Change event object emitted by URRadioComponent and URRadioGroupDirective. */
export declare class URRadioChange {
    /** The URRadioButton that emits the change event. */
    source: URRadioButtonBaseDirective;
    /** The value of the URRadioButton. */
    value: any;
    constructor(
    /** The URRadioButton that emits the change event. */
    source: URRadioButtonBaseDirective, 
    /** The value of the URRadioButton. */
    value: any);
}
/**
 * Injection token that can be used to
 * inject instances of `URRadioGroupDirective`. It serves as
 * alternative token to the actual `URRadioGroupDirective` class which could cause unnecessary
 * retention of the class and its component metadata.
 */
export declare const UR_RADIO_GROUP: InjectionToken<URRadioGroupBaseDirective<URRadioButtonBaseDirective>>;
/**
 * Base class with all of the `URRadioGroupDirective` functionality.
 * @docs-private
 */
export declare abstract class URRadioGroupBaseDirective<T extends URRadioButtonBaseDirective> implements AfterContentInit, ControlValueAccessor {
    private readonly _changeDetector;
    /**
     * Event emitted when the group value changes.
     * Change events are only emitted when the value changes due to user interaction with
     * a radio button (the same behavior as `<input type-"radio">`).
     */
    readonly radioChange: EventEmitter<URRadioChange>;
    /** Selected value for the radio group. */
    private _value;
    /** The HTML name attribute applied to radio buttons in this group. */
    private _name;
    /** The currently selected radio button. Should match value. */
    private _selected;
    /** Whether the `value` has been set to its initial value. */
    private _isInitialized;
    /** Whether the labels should appear after or before the radio-buttons. Defaults to 'after' */
    private _labelPosition;
    /** Whether the radio buttons should appear after or underneath each other. Defaults to 'inline' */
    private _buttonPosition;
    /** Whether the radio group is disabled. */
    private _disabled;
    /** Whether the radio group is required. */
    private _required;
    /** The method to be called in order to update ngModel */
    _controlValueAccessorChangeFn: (value: any) => void;
    /**
     * onTouch function registered via registerOnTouch (ControlValueAccessor).
     * @docs-private
     */
    onTouched: () => any;
    /** Child radio buttons. */
    abstract _radios?: QueryList<T>;
    /** Name of the radio button group. All radio buttons inside this group will use this name. */
    get name(): string;
    set name(value: string);
    /** Whether the labels should appear after or before the radio-buttons. Defaults to 'after' */
    get labelPosition(): 'before' | 'after';
    set labelPosition(value: "before" | "after");
    /** Whether the radio buttons should appear inline or underneath each other. Defaults to 'after' */
    get buttonPosition(): 'inline' | 'after';
    set buttonPosition(v: "inline" | "after");
    /**
     * Value for the radio-group. Should equal the value of the selected radio button if there is
     * a corresponding radio button with a matching value. If there is not such a corresponding
     * radio button, this value persists to be applied in case a new radio button is added with a
     * matching value.
     */
    get value(): any;
    set value(newValue: any);
    _checkSelectedRadioButton(): void;
    /**
     * The currently selected radio button. If set to a new radio button, the radio group value
     * will be updated to match the new selected button.
     */
    get selected(): T | null;
    set selected(selected: T | null);
    /** Whether the radio group is disabled */
    get disabled(): boolean;
    set disabled(value: boolean);
    /** Whether the radio group is required */
    get required(): boolean;
    set required(value: boolean);
    constructor(_changeDetector: ChangeDetectorRef);
    /**
     * Initialize properties once content children are available.
     * This allows us to propagate relevant attributes to associated buttons.
     */
    ngAfterContentInit(): void;
    /**
     * Mark this group as being "touched" (for ngModel). Meant to be called by the contained
     * radio buttons upon their blur.
     */
    _touch(): void;
    private _updateRadioButtonNames;
    /** Updates the `selected` radio button from the internal _value state. */
    private _updateSelectedRadioFromValue;
    /** Dispatch change event with current selection and group value. */
    _emitChangeEvent(): void;
    _markRadiosForCheck(): void;
    /**
     * Sets the model value. Implemented as part of ControlValueAccessor.
     * @param value
     */
    writeValue(value: any): void;
    /**
     * Registers a callback to be triggered when the model value changes.
     * Implemented as part of ControlValueAccessor.
     * @param fn Callback to be registered.
     */
    registerOnChange(fn: (value: any) => void): void;
    /**
     * Registers a callback to be triggered when the control is touched.
     * Implemented as part of ControlValueAccessor.
     * @param fn Callback to be registered.
     */
    registerOnTouched(fn: any): void;
    /**
     * Sets the disabled state of the control. Implemented as a part of ControlValueAccessor.
     * @param isDisabled Whether the control should be disabled.
     */
    setDisabledState(isDisabled: boolean): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<URRadioGroupBaseDirective<any>, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<URRadioGroupBaseDirective<any>, never, never, { "name": { "alias": "name"; "required": false; }; "labelPosition": { "alias": "labelPosition"; "required": false; }; "buttonPosition": { "alias": "buttonPosition"; "required": false; }; "value": { "alias": "value"; "required": false; }; "selected": { "alias": "selected"; "required": false; }; "disabled": { "alias": "disabled"; "required": false; }; "required": { "alias": "required"; "required": false; }; }, { "radioChange": "radioChange"; }, never, never, true, never>;
}
/**
 * A group of radio buttons. May contain one or more `<ur-radio-button>` elements.
 */
export declare class URRadioGroupDirective extends URRadioGroupBaseDirective<URRadioButtonComponent> {
    _radios?: QueryList<URRadioButtonComponent>;
    static ɵfac: i0.ɵɵFactoryDeclaration<URRadioGroupDirective, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<URRadioGroupDirective, "ur-radio-group", ["urRadioGroup"], {}, {}, ["_radios"], never, true, never>;
}
/**
 * Base class with all of the `URRadioButton` functionality.
 * @docs-private
 */
export declare abstract class URRadioButtonBaseDirective implements OnInit, AfterViewInit, OnDestroy {
    elementRef: ElementRef;
    protected _changeDetector: ChangeDetectorRef;
    private readonly _focusMonitor;
    private readonly _radioDispatcher;
    private readonly _uniqueId;
    /** The native `<input type=radio>` element */
    _inputElement?: ElementRef<HTMLInputElement>;
    /** The unique ID for the radio button. */
    id: string;
    /** Analog to HTML 'name' attribute used to group radios for unique selection. */
    name?: string;
    /** Used to set the 'aria-label' attribute on the underlying input element. */
    ariaLabel?: string;
    /** The 'aria-labelledby' attribute takes precedence as the element's text alternative. */
    ariaLabelledby?: string;
    /** The 'aria-describedby' attribute is read after the element's label and field type. */
    ariaDescribedby?: string;
    /** Whether this radio button is checked. */
    get checked(): boolean;
    set checked(value: boolean);
    /** The value of this radio button. */
    get value(): any;
    set value(value: any);
    /** Whether the label should appear after or before the radio button. Defaults to 'after' */
    get labelPosition(): 'before' | 'after';
    set labelPosition(value: "before" | "after");
    private _labelPosition?;
    /** Whether the radio button is disabled. */
    get disabled(): boolean;
    set disabled(value: boolean);
    /** Whether the radio button is invalid */
    get invalid(): boolean;
    set invalid(value: boolean);
    /** Whether the radio button is required. */
    get required(): boolean;
    set required(value: boolean);
    /** Whether the radio should hover */
    get hover(): boolean;
    set hover(value: boolean);
    /**
     * Event emitted when the checked state of this radio button changes.
     * Change events are only emitted when the value changes due to user interaction with
     * the radio button (the same behavior as `<input type-"radio">`).
     */
    readonly radioChange: EventEmitter<URRadioChange>;
    /** The parent radio group. May or may not be present. */
    radioGroup: URRadioGroupBaseDirective<URRadioButtonBaseDirective>;
    /** ID of the native input element inside `<mat-radio-button>` */
    get inputId(): string;
    /** Whether this radio is checked. */
    private _checked;
    /** Whether the radio group is disabled. */
    private _disabled;
    /** Whether the radio group is required. */
    private _required;
    private _invalid;
    /** Value assigned to this radio. */
    private _value;
    private _hover;
    /** Unregister function for _radioDispatcher */
    private readonly _removeUniqueSelectionListener;
    protected constructor(radioGroup: URRadioGroupBaseDirective<URRadioButtonBaseDirective>, elementRef: ElementRef, _changeDetector: ChangeDetectorRef, _focusMonitor: FocusMonitor, _radioDispatcher: UniqueSelectionDispatcher);
    /** Focuses the radio button. */
    focus(options?: FocusOptions, origin?: FocusOrigin): void;
    /**
     * Marks the radio button as needing checking for change detection.
     * This method is exposed because the parent radio group will directly
     * update bound properties of the radio button.
     */
    _markForCheck(): void;
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
    /** Dispatch change event with current value. */
    private _emitChangeEvent;
    _onInputClick(event: Event): void;
    /** Triggered when the radio button receives an interaction from the user. */
    _onInputInteraction(event: Event): void;
    /** Sets the disabled state and marks for check if a change occurred. */
    protected _setDisabled(value: boolean): void;
    /** Sets the invalid state */
    protected _setInvalid(value: boolean): void;
    /** Sets the hover state */
    protected _setHover(value: boolean): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<URRadioButtonBaseDirective, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<URRadioButtonBaseDirective, never, never, { "id": { "alias": "id"; "required": false; }; "name": { "alias": "name"; "required": false; }; "ariaLabel": { "alias": "aria-label"; "required": false; }; "ariaLabelledby": { "alias": "aria-labelledby"; "required": false; }; "ariaDescribedby": { "alias": "aria-describedby"; "required": false; }; "checked": { "alias": "checked"; "required": false; }; "value": { "alias": "value"; "required": false; }; "labelPosition": { "alias": "labelPosition"; "required": false; }; "disabled": { "alias": "disabled"; "required": false; }; "invalid": { "alias": "invalid"; "required": false; }; "required": { "alias": "required"; "required": false; }; "hover": { "alias": "hover"; "required": false; }; }, { "radioChange": "radioChange"; }, never, never, true, never>;
}
/**
 * A radio-button. Typically placed inside of `<ur-radio-group>` elements.
 */
export declare class URRadioButtonComponent extends URRadioButtonBaseDirective {
    constructor(radioGroup: URRadioGroupDirective, elementRef: ElementRef, changeDetector: ChangeDetectorRef, focusMonitor: FocusMonitor, radioDispatcher: UniqueSelectionDispatcher);
    static ɵfac: i0.ɵɵFactoryDeclaration<URRadioButtonComponent, [{ optional: true; }, null, null, null, null]>;
    static ɵcmp: i0.ɵɵComponentDeclaration<URRadioButtonComponent, "ur-radio-button", ["urRadioButton"], {}, {}, never, ["*"], true, never>;
}
