import { AnimationEvent } from '@angular/animations';
import { AfterViewInit, ChangeDetectorRef, ElementRef, EventEmitter, OnChanges, OnDestroy, SimpleChanges } from '@angular/core';
import { Subject } from 'rxjs';
import * as i0 from "@angular/core";
export type URExpansionPanelState = 'expanded' | 'collapsed';
export declare class URExpansionPanelComponent implements AfterViewInit, OnChanges, OnDestroy {
    private readonly _changeDetectorRef;
    private readonly _document;
    /** An event emitted after the body's expansion animation happens. */
    readonly afterExpand: EventEmitter<void>;
    /** An event emitted after the body's collapse animation happens. */
    readonly afterCollapse: EventEmitter<void>;
    /** An event emitted whenever expanded property have changed. */
    readonly expandedChange: EventEmitter<boolean>;
    /** Event emitted every time the panel is closed. */
    readonly closed: EventEmitter<void>;
    /** Event emitted every time the panel is opened. */
    readonly opened: EventEmitter<void>;
    /** Whether the toggle indicator should be hidden. */
    get hideToggle(): boolean;
    set hideToggle(value: boolean);
    private _hideToggle;
    private readonly urExpandedClassName;
    /** Whether the panel is expanded. */
    get expanded(): boolean;
    set expanded(expanded: boolean);
    private _expanded;
    /** Whether the panel is disabled. */
    get disabled(): boolean;
    set disabled(disabled: boolean);
    private _disabled;
    /** The unique panel id. */
    readonly id: string;
    /** The unique header id. */
    _headerId: string;
    /** Stream that emits for when animations are done. */
    readonly _bodyAnimationDone: Subject<AnimationEvent>;
    /** Stream that emits for changes in `@Input` properties. */
    readonly _inputChanges: Subject<SimpleChanges>;
    /** Element containing the panel's user-provided content. */
    _body?: ElementRef<HTMLElement>;
    constructor(_document: any, _changeDetectorRef: ChangeDetectorRef);
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
    ngOnChanges(changes: SimpleChanges): void;
    /** Gets the expanded state string. */
    _getExpandedState(): URExpansionPanelState;
    /** Toggles the expanded state of the accordion item. */
    toggle(): void;
    /** Sets the expanded state of the accordion item to false. */
    close(): void;
    /** Sets the expanded state of the accordion item to true. */
    open(): void;
    /** Checks whether the expansion panel's content contains the currently-focused element. */
    _containsFocus(): boolean;
    static ɵfac: i0.ɵɵFactoryDeclaration<URExpansionPanelComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<URExpansionPanelComponent, "ur-expansion-panel", never, { "hideToggle": { "alias": "hideToggle"; "required": false; }; "expanded": { "alias": "expanded"; "required": false; }; "disabled": { "alias": "disabled"; "required": false; }; }, { "afterExpand": "afterExpand"; "afterCollapse": "afterCollapse"; "expandedChange": "expandedChange"; "closed": "closed"; "opened": "opened"; }, never, ["ur-expansion-panel-header", "ur-expansion-panel-selectable-header", "*"], true, never>;
}
