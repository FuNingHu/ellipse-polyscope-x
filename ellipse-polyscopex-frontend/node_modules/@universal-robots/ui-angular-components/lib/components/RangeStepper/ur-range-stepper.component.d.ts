import { EventEmitter, OnDestroy, OnInit } from '@angular/core';
import { Observable } from 'rxjs';
import * as i0 from "@angular/core";
/**
 * Dumb/presentational component.
 * Input stepper UI. Delegates user inputs.
 *
 * Configurable properties:
 * - enabled/disabled
 * - label text
 * - displayed precision
 * - unit
 * - clamping
 *
 * Example template usage:
 *  <ur-range-stepper
 *   (clickedIncrease)="handleSpeedIncrement()"
 *   (clickedDecrease)="handleSpeedDecrement()"
 *   (pressedIncrease)="handleSpeedIncrementJump()"
 *   (pressedDecrease)="handleSpeedDecrementJump()"
 *   [value$]="robotSpeed$"
 *   label="Speed"
 *   precision="2"
 *   unit="%"
 *   min="1"
 *   max="100"
 *   [disabled]="disabled"
 *  ></ur-range-stepper>
 */
export declare class URRangeStepperComponent implements OnInit, OnDestroy {
    /**
     * Increase/decrease 'pressed' events stream.
     */
    private readonly interval$;
    private intervalSubscription?;
    private readonly subscriptions;
    /**
     * Whether the control is disabled or not.
     */
    disabled: boolean;
    /**
     * Current ranged value.
     */
    value$: Observable<number>;
    /**
     * Displayed label.
     */
    label?: string;
    /**
     * Number of decimal digits to be used when displaying the ranged value.
     */
    precision: number;
    /**
     * The unit type to be used when displaying the ranged value, e.g. '%', 'Meters', etc.
     */
    unit: string;
    /**
     * Clamp
     */
    min: number;
    /**
     * Clamp
     */
    max: number;
    /**
     * Value change request event emitter.
     * Should be called when the user CLICKS the decrease button.
     */
    clickedDecrease: EventEmitter<Event>;
    /**
     * Value change request event emitter.
     * Should be called when the user CLICKS the increase button.
     */
    clickedIncrease: EventEmitter<Event>;
    /**
     * Value change request event emitter.
     * Should be called when the user HOLDS DOWN the decrease button.
     */
    pressedDecrease: EventEmitter<Event>;
    /**
     * Value change request event emitter.
     * Should be called when the user HOLDS DOWN the increase button.
     */
    pressedIncrease: EventEmitter<Event>;
    value: number;
    ngOnInit(): void;
    ngOnDestroy(): void;
    /**
     * Initiates a stream of 'decrease' events.
     */
    onPointerDownDecrease(e: Event): void;
    /**
     * Initiates a stream of 'increase' events.
     */
    onPointerDownIncrease(e: Event): void;
    /**
     * Ends any stream of 'decrease/increase' events.
     */
    onPointerLeave(): void;
    /**
     * Constructs the digitsInfo parameter for the DecimalPipe.
     */
    getDigitsInfo(decimals: number): string;
    static ɵfac: i0.ɵɵFactoryDeclaration<URRangeStepperComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<URRangeStepperComponent, "ur-range-stepper", never, { "disabled": { "alias": "disabled"; "required": false; }; "value$": { "alias": "value$"; "required": false; }; "label": { "alias": "label"; "required": false; }; "precision": { "alias": "precision"; "required": false; }; "unit": { "alias": "unit"; "required": false; }; "min": { "alias": "min"; "required": false; }; "max": { "alias": "max"; "required": false; }; }, { "clickedDecrease": "clickedDecrease"; "clickedIncrease": "clickedIncrease"; "pressedDecrease": "pressedDecrease"; "pressedIncrease": "pressedIncrease"; }, never, never, true, never>;
}
