import { Overlay } from '@angular/cdk/overlay';
import { ComponentType } from '@angular/cdk/portal';
import { TemplateRef } from '@angular/core';
import { CloseDialog, CloseReasonType, ConfirmationDialog, DialogConfig, DialogError } from '@universal-robots/ui-models';
import { DialogRef } from '../ref';
import { LocalStorageList } from '../utils/local-storage';
import * as i0 from "@angular/core";
export declare class URPortalService {
    private readonly overlay;
    localStorageHelper: LocalStorageList<string>;
    constructor(overlay: Overlay);
    open<T, C = any>(componentOrTemplate: ComponentType<any> | TemplateRef<any>, customData?: C, config?: DialogConfig): DialogRef<T>;
    /**
     * close the current dialog from portal - not recommended but possible
     * @param customData T
     */
    close(customData: CloseDialog<unknown>): void;
    /**
     * close the dialog based on provided id from portal
     * @param id The id for the dialog to close
     */
    closeById(id: string): void;
    /**
     * Closes dialogs based on the error type. This is used to close dialogs
     * @param error DialogError
     */
    closeByErrorType(error: DialogError): Promise<string | undefined>;
    /**
    
     * Force closes all open dialogs if for some reason all overlays should be destroyed
     * @warning this will close all dialogs without any feedback - not recommended
     */
    forceCloseAll(): void;
    openRequiredConfirmationDialog(dialogContent: ConfirmationDialog): Promise<CloseReasonType>;
    openConfirmationDialog(dialogContent: ConfirmationDialog): Promise<CloseReasonType>;
    private openConfirmation;
    private attachContainer;
    private createInjector;
    private attachContent;
    private getOverlayConfig;
    private createOverlay;
    static ɵfac: i0.ɵɵFactoryDeclaration<URPortalService, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<URPortalService>;
}
