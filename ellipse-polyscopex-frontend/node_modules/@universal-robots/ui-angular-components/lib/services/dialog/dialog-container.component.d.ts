import { BasePortalOutlet, CdkPortalOutlet, ComponentPortal, TemplatePortal } from '@angular/cdk/portal';
import { ComponentRef, ElementRef, EmbeddedViewRef, NgZone, OnDestroy } from '@angular/core';
import { DialogConfig } from '@universal-robots/ui-models';
import { DialogAnimationEvent } from '../../animations';
import { DialogRef } from '../../ref';
import * as i0 from "@angular/core";
export declare function throwDialogContentAlreadyAttachedError(): void;
/**
 * Internal component that wraps user-provided dialog content.
 * @docs-private
 */
export declare class URDialogContainerComponent<C extends DialogConfig = DialogConfig> extends BasePortalOutlet implements OnDestroy {
    private readonly localStorageHelper;
    protected elementRef: ElementRef<any>;
    protected config: any;
    protected dialogRef: DialogRef<any>;
    protected _ngZone: NgZone;
    /** The portal outlet inside of this container into which the dialog content will be loaded.
     * need to be static because the content is loaded in the constructor
     * and the view is not initialized yet.
     */
    portalOutlet: CdkPortalOutlet;
    /**
     * Focus trap that will be used to trap and manage focus within the dialog.
     */
    private focusTrap?;
    constructor();
    animationEffect: (event: CustomEvent<DialogAnimationEvent>) => void;
    /** */
    protected contentAttached(): void;
    /**
     * Can be used by child classes to customize the initial focus
     * capturing behavior (e.g. if it's tied to an animation).
     */
    protected activateFocusTrap(): void;
    /**
     * Pauses the focus trap. This is used when multiple dialogs are open and
     * one of them is not the active dialog. The focus trap will be paused
     * until the dialog is activated again.
     */
    protected pauseFocusTrap(): void;
    /**
     * Recaptures the focus trap. This is used when the dialog is activated again
     * after being paused.
     */
    protected reActivateFocusTrap(): void;
    ngOnDestroy(): void;
    /**
     * Attach a ComponentPortal as content to this dialog container.
     * @param portal Portal to be attached as the dialog content.
     */
    attachComponentPortal<T>(portal: ComponentPortal<T>): ComponentRef<T>;
    /**
     * Attach a TemplatePortal as content to this dialog container.
     * @param portal Portal to be attached as the dialog content.
     */
    attachTemplatePortal<T>(portal: TemplatePortal<T>): EmbeddedViewRef<T>;
    /** Sets up the focus trap. */
    private initializeFocusTrap;
    private readonly allowOutsideClick;
    static ɵfac: i0.ɵɵFactoryDeclaration<URDialogContainerComponent<any>, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<URDialogContainerComponent<any>, "ur-dialog-container", never, {}, {}, never, never, true, never>;
}
