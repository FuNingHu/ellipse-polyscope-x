import { TabInputModel } from '../../../common';
import { Acceleration, AngularAcceleration, AngularSpeed, Length, Speed } from '../../../values';
import { ProgramNode } from '../../program.node';
import { URVariable } from '../../ur-symbol';
import { WaypointTabInputModel } from './move-to.node';
import { NodeType } from './program-node-type';
export interface MovePNode extends ProgramNode {
    type: NodeType.MOVE_P;
    parameters: {
        motionProfile: string | undefined;
        variable: TabInputModel<URVariable | undefined>;
        waypoint?: WaypointTabInputModel;
        advanced: MovePAdvancedSettings;
    };
}
export interface MovePAdvancedSettings {
    speed: MovePSpeedSettings;
    transform: MovePTransformSettings;
    blend: MovePBlendSettings;
}
export interface MovePSpeedSettings {
    speed: TabInputModel<Speed | AngularSpeed>;
    acceleration: TabInputModel<Acceleration | AngularAcceleration>;
}
export interface MovePBlendSettings {
    radius: TabInputModel<Length>;
}
export interface MovePTransformSettings {
    transform: boolean;
    poseVariable?: URVariable;
}
