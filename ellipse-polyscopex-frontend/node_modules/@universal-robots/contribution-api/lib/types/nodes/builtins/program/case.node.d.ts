import { ProgramNode } from '../../program.node';
import { URFunction, URModule } from '../../ur-symbol';
import { NodeType } from './program-node-type';
export interface CaseNode extends ProgramNode {
    type: NodeType.CASE | NodeType.DEFAULT_CASE;
    parameters: {
        isDefaultCase: boolean;
        value: number | string | undefined;
        selectedModule?: URModule;
        selectedFunction?: URFunction;
    };
}
